# 고급 분석 로직 설명

## 개요

역대 로또 번호를 깊이 분석하여 더 정교한 추천을 제공하는 시스템으로 업그레이드되었습니다.

## 주요 개선 사항

### 1. **PatternAnalyzer (패턴 분석기)**

역대 데이터에서 다양한 패턴을 추출합니다:

#### 번호별 출현 빈도
- 각 번호(1-45)의 출현 횟수 분석
- 정규화된 빈도 계산 (0~1 범위)

#### 번호 쌍 분석
- 두 번호가 함께 나온 빈도 분석
- 예: (1, 2), (3, 5) 등이 함께 나온 횟수

#### 번호 3개 조합 분석
- 세 번호가 함께 나온 빈도 분석
- 더 복잡한 패턴 파악

#### 합계 패턴
- 당첨 번호 합계의 통계 분석
- 평균, 중앙값, 표준편차, 사분위수

#### 홀짝 비율 패턴
- 홀수/짝수 개수 분포 분석
- 가장 많이 나온 홀수 개수 파악

#### 연속 번호 패턴
- 연속된 번호가 나온 빈도
- 평균 연속 번호 개수

#### 고저 비율 패턴
- 저번호(1-22)와 고번호(23-45) 비율 분석

#### 번호 간격 패턴
- 번호 사이의 간격 분석
- 평균 간격, 최소/최대 간격

#### 최근 트렌드
- 최근 20회차 분석
- Hot Numbers: 최근 자주 나온 번호
- Cold Numbers: 최근 안 나온 번호

### 2. **AdvancedRecommendationEngine (고급 추천 엔진)**

#### 후보 생성 전략
1. **Hot/Cold 번호 반영**
   - Hot 번호 2-3개 포함 (최근 트렌드)
   - Cold 번호 1-2개 포함 (반등 가능성)

2. **빈도 기반 샘플링**
   - 나머지 번호는 출현 빈도 기반 가중치 샘플링

3. **최적 조합 선택**
   - 50개 후보 생성
   - 각 후보의 최적성 점수 계산
   - 최고 점수 조합 선택

#### 최적 조합 점수 계산

다음 요소들을 종합하여 점수를 계산합니다:

1. **빈도 점수 (30%)**
   - 번호별 출현 빈도 평균

2. **쌍 점수 (20%)**
   - 번호 쌍이 함께 나온 빈도

3. **합계 점수 (15%)**
   - 역대 평균 합계와의 유사도

4. **홀짝 비율 점수 (15%)**
   - 역대 가장 많이 나온 홀짝 비율과의 일치도

5. **연속 번호 점수 (10%)**
   - 역대 평균 연속 번호 개수와의 일치도

6. **고저 비율 점수 (10%)**
   - 역대 가장 많이 나온 고저 비율과의 일치도

#### 최종 신뢰도 계산

다음 가중치로 최종 신뢰도를 계산합니다:

- `pattern_score`: 25% (가장 중요)
- `pair_score`: 15%
- `frequency_score`: 15%
- `sum_score`: 10%
- `odd_even_score`: 10%
- `trend_score`: 10%
- `consecutive_score`: 5%
- `high_low_score`: 5%
- `weather_idx`: 3%
- `economic_idx`: 2%

## 사용 방법

기존 API와 동일하게 사용하면 자동으로 고급 분석이 적용됩니다:

```bash
GET /api/v1/recommendations/numbers?count=5
```

## 분석 결과 예시

```json
{
  "numbers": [3, 11, 19, 27, 34, 42],
  "confidence": 0.78,
  "features": {
    "frequency_score": 0.65,
    "pattern_score": 0.82,
    "pair_score": 0.70,
    "sum_score": 0.85,
    "odd_even_score": 0.90,
    "consecutive_score": 0.75,
    "high_low_score": 0.80,
    "trend_score": 0.60,
    "weather_idx": 0.24,
    "economic_idx": 0.15
  },
  "explanation": "과거 출현 빈도가 높은 번호를 포함했습니다. 역대 패턴과 유사한 조합입니다. 최근 트렌드를 반영했습니다."
}
```

## 성능 향상

- **이전**: 단순 빈도 기반 추천
- **현재**: 다차원 패턴 분석 기반 추천
- **정확도**: 더 정교한 점수 계산으로 신뢰도 향상

## 향후 개선 가능 항목

1. 머신러닝 모델 통합 (XGBoost, LightGBM)
2. 시계열 분석 (ARIMA, Prophet)
3. 앙상블 방법 (여러 알고리즘 조합)
4. 실시간 학습 (새로운 당첨 결과 반영)

